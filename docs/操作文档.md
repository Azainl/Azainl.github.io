# 个人博客操作文档

> 适用项目：`D:\Code\blog`
> 线上地址：<https://azainl.github.io>
> 代码仓库：<https://github.com/Azainl/Azainl.github.io>

本文档覆盖：本地开发、写文章、改配置、改设计、部署发布、日常维护和常见问题。

---

## 1. 快速开始

```powershell
# 进入项目目录
cd D:\Code\blog

# 安装依赖（只需一次；如果之前装过可跳过）
npm.cmd install

# 本地开发：启动后浏览器打开 http://localhost:4321
npm.cmd run dev

# 构建线上版本（输出到 dist/）
npm.cmd run build

# 预览构建结果
npm.cmd run preview
```

> **本机注意**：PowerShell 默认禁止运行 `npm.ps1` 脚本，所以本机统一用
> `npm.cmd` 而不是 `npm`。如果执行 `npm` 报"禁止运行脚本"，换 `npm.cmd` 即可。

发布到线上只需一条命令组合：

```powershell
git add -A
git commit -m "更新内容"
git push
```

推送后 GitHub Actions 会自动构建并部署，约 1 分钟后线上更新。

---

## 2. 项目结构

```
D:\Code\blog
├─ public/                  # 静态资源，原样复制到线上
│  ├─ favicon.svg           # 站点图标
│  └─ og.svg                # 分享到社交平台时的预览图
├─ src/
│  ├─ components/           # 组件
│  │  ├─ Header.astro       # 页头（返回按钮 + 导航 + 搜索 + 主题切换）
│  │  └─ Footer.astro       # 页脚（版权 + RSS 链接）
│  ├─ content/
│  │  └─ posts/             # ★ 所有文章都放在这里（Markdown）
│  ├─ data/tags.ts          # 标签的一句话描述（可自定义）
│  ├─ layouts/
│  │  ├─ BaseLayout.astro   # 全站基础模板（head、导航、页脚）
│  │  └─ PostLayout.astro   # 文章页模板
│  ├─ pages/                # 路由页面
│  │  ├─ index.astro        # 首页（文章列表 + 翻页）
│  │  ├─ about.astro        # 关于页
│  │  ├─ 404.astro          # 404 页面
│  │  ├─ rss.xml.js         # RSS 订阅源（自动生成）
│  │  ├─ search.astro       # 搜索页（实时检索）
│  │  ├─ search.json.ts     # 搜索索引（构建时自动生成）
│  │  ├─ posts/[slug].astro # 文章详情页（自动生成）
│  │  └─ tags/              # 标签页（自动生成）
│  ├─ scripts/              # 客户端脚本（搜索逻辑、滚动动效）
│  ├─ styles/global.css     # ★ 全站样式与设计令牌
│  ├─ consts.ts             # ★ 站点信息集中配置
│  ├─ content.config.ts     # 文章数据校验规则（Zod）
│  └─ utils/                # 日期格式化、阅读时长计算
├─ scripts/                 # 辅助脚本（截图、检查，可选）
├─ .github/workflows/       # GitHub Pages 自动部署配置
├─ astro.config.mjs         # Astro 配置（站点域名等）
├─ netlify.toml             # 备用：Netlify 部署配置
└─ vercel.json              # 备用：Vercel 部署配置
```

带 ★ 的文件是日常最常碰到的三个：写文章看 `content/posts/`，
改站点信息看 `consts.ts`，改外观看 `global.css`。

---

## 3. 写文章（最常用）

### 3.1 新建一篇文章

在 `src/content/posts/` 下新建一个 Markdown 文件即可。文件名就是文章的网址
（slug），建议用英文或拼音，例如 `my-first-post.md` 对应网址
`https://azainl.github.io/posts/my-first-post/`。

文件头部必须写 frontmatter（两行 `---` 之间的内容）：

```md
---
title: "文章标题"
description: "文章摘要，显示在首页列表、搜索引擎和分享卡片里"
date: 2026-08-03
tags: ["随笔", "思考"]
draft: false
---

正文从这里开始，支持标准的 Markdown 语法。
```

### 3.2 frontmatter 字段说明

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `title` | 是 | 文章标题 |
| `description` | 是 | 摘要，建议一句话说清文章内容 |
| `date` | 是 | 发布日期，格式 `YYYY-MM-DD` |
| `tags` | 否 | 标签数组，最多随意，会生成 `/tags/标签名/` 页面 |
| `draft` | 否 | `true` 时文章只在本地可见，不会发布 |
| `updated` | 否 | 修改日期，格式同 `date` |

字段缺失或日期格式错误时，构建会直接报错（这是保护机制，防止坏数据上线）。
标签页会显示每个标签的一句话描述，定义在 `src/data/tags.ts` 里，可自由增改。

### 3.3 支持的 Markdown 语法

标题、段落、**加粗**、*斜体*、[链接](https://example.com)、
`行内代码`、列表、引用、表格、代码块（带语法高亮）都支持。

```md
## 二级标题

### 三级标题

> 这是引用，会显示成带左边线的样式

- 列表项一
- 列表项二

1. 有序列表一
2. 有序列表二

| 表头一 | 表头二 |
| --- | --- |
| 内容 | 内容 |

    ```js
    console.log('代码块自动高亮');
    ```
```

### 3.4 插入图片

把图片文件放进 `public/images/`（没有就新建），然后在文章里写：

```md
![图片说明](/images/文件名.png)
```

图片会按原样发布，建议先用工具压缩一下尺寸，太大的图片会影响加载速度。

### 3.5 阅读时长

阅读时长是自动计算的（中文按 400 字/分钟估算），无需手动填写。

### 3.6 草稿与预览

把 `draft: true` 的文章推送到 GitHub 不会出现在线上；在本地 `npm.cmd run dev`
时仍然可以看到，方便先写后发。

---

## 4. 修改站点信息

所有站点配置集中在 `src/consts.ts`，只改这一个文件即可：

```ts
export const SITE = {
  name: 'Azain',                    // 站点名（页脚等位置显示）
  title: 'Azain · 个人博客',        // 浏览器标签页标题
  description: '一个关于代码、阅读与生活的个人博客。', // 站点简介
  author: 'Azain',                  // 作者名（关于页使用）
  email: '2013386765@qq.com',       // 联系邮箱（关于页显示）
  url: 'https://azainl.github.io',  // 站点域名，改域名时必须同步改
  locale: 'zh-CN',
};
```

修改后执行 `npm.cmd run build` 确认没有报错，再 `git push` 发布。

另外两处可选的独立修改：

- 页头左上角的文字：`src/components/Header.astro` 里的 `山<span>月</span>`；
  现在站点名已统一为 Azain，如果改了 `consts.ts` 里的 `name`，这里也要同步。
- 站点图标：替换 `public/favicon.svg`；分享预览图替换 `public/og.svg`。

---

## 5. 修改设计（外观）

全站样式都在 `src/styles/global.css`，用 CSS 变量（设计令牌）统一管理。

### 5.1 颜色

`:root { }` 是浅色主题的变量，`.dark { }` 是深色主题的变量，两套对应关系一致：

| 变量 | 作用 |
| --- | --- |
| `--bg` | 页面背景色 |
| `--ink` | 正文文字色 |
| `--ink-2` | 次要文字色（摘要、正文说明） |
| `--ink-3` | 弱文字色（日期、年份） |
| `--accent` | 全站强调色（链接、点缀、高亮） |
| `--line` | 分隔线颜色 |
| `--code-bg` | 代码块背景 |

改颜色只需改变量值，全站自动生效。注意保持：**全站只用一个强调色**，
深浅两套都改，避免出现浅色下的强调色在深色下看不清。

### 5.2 字体

字体文件在 `src/layouts/BaseLayout.astro` 顶部通过 `@fontsource` 包引入
（自托管，不依赖外部服务）：

```ts
import '@fontsource-variable/geist';              // 界面无衬线字体
import '@fontsource-variable/source-serif-4';     // 标题衬线字体
```

字体栈定义在 `global.css` 的 `--font-sans` / `--font-serif` / `--font-mono`。
想换字体：先 `npm.cmd install @fontsource-variable/字体名`，改 import 和变量即可。

### 5.3 布局与圆角

- `--container`（文章宽度）和 `--container-wide`（列表页宽度）控制页面宽度
- `--radius` / `--radius-pill` / `--radius-inline` 控制圆角体系，
  保持同一套圆角规则，不要混用
- 首页文章列表、文章页排版分别在 `src/pages/index.astro` 和
  `src/layouts/PostLayout.astro`

### 5.4 深浅主题

- 首次访问跟随系统偏好，之后记住手动选择（存在浏览器 localStorage）
- 主题初始化脚本在 `BaseLayout.astro` 的 `<head>` 里，切换按钮在
  `Header.astro`
- 新增配色时记得同时写 `:root` 和 `.dark` 两套

---

## 6. 页面与路由

### 6.1 现有路由

| 路径 | 来源 | 说明 |
| --- | --- | --- |
| `/` | `src/pages/index.astro` | 首页，按年份分组 + 每页 4 篇翻页（`?page=2` 直达） |
| `/posts/文章名/` | 自动生成 | 每篇文章一个详情页 |
| `/tags/` | `src/pages/tags/index.astro` | 全部标签 |
| `/tags/标签名/` | 自动生成 | 某标签下的文章 |
| `/search/` | `src/pages/search.astro` | 搜索页（移动端入口与分享链接用） |
| `/about/` | `src/pages/about.astro` | 关于页 |
| `/rss.xml` | 自动生成 | RSS 订阅源 |
| `/sitemap-index.xml` | 自动生成 | 搜索引擎用的站点地图 |
| `/404` | `src/pages/404.astro` | 页面不存在时 |

### 6.2 新增一个页面

在 `src/pages/` 下新建 `xxx.astro`，套用基础模板：

```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="页面标题" description="页面描述">
  <div class="page-head">
    <h1>页面标题</h1>
    <p>页面说明文字。</p>
  </div>
  <div class="container">
    页面内容……
  </div>
</BaseLayout>
```

保存后访问 `/xxx/`（文件名即路径）。

### 6.3 修改导航和页脚

- 导航链接：`src/components/Header.astro` 里的 `links` 数组
- 页脚内容：`src/components/Footer.astro`
- 页头左上角的返回按钮：非首页自动显示，点击返回上一页；
  从站外直接进入时点击回到首页

### 6.4 搜索功能

- **搜索框就在主界面页头**，每个页面都有：输入即出下拉结果，
  命中关键词高亮，回车打开首条结果，↑/↓ 键选择，Esc 关闭
- `/search/` 页面保留，用于移动端图标入口和可分享的搜索链接
  （例如 `/search/?q=Astro`）
- 搜索覆盖标题、标签、摘要和正文；索引在构建时自动生成（`search.json`）

### 6.5 动效

- 内容进入视口时渐显（带错峰延迟）；浏览器开启"减少动态效果"或
  无 JS 时自动降级为直接显示
- 悬停微交互：导航下划线生长、标签上浮、主题图标旋转、列表行高亮
- 全部用原生 CSS 过渡/动画实现，不依赖第三方动画库
- **页面切换动画**：全站启用 View Transitions，点击文章时标题会从列表位置
  平滑"飞"到文章页标题位置（返回时反向飞回），页面切换带淡入淡出
- 浏览器返回键遇到路由交换异常时会自动整页刷新兜底，保证页面不空白

### 6.6 首页翻页与滚动条

- 首页每页显示 4 篇文章（`src/pages/index.astro` 里的 `POSTS_PER_PAGE`
  可调整），翻页带淡入淡出动画，页码变化同步到 URL
- 页面右侧滚动条已定制样式，随深浅主题自动切换

---

## 7. 部署与发布

### 7.1 发布流程（正常情况）

```powershell
cd D:\Code\blog
git add -A
git commit -m "写点什么"
git push
```

推送后：

1. GitHub Actions 自动运行 `.github/workflows/deploy.yml`
2. 构建生成 `dist/`，上传并发布到 Pages
3. 约 1 分钟后访问 <https://azainl.github.io> 看到更新

### 7.2 查看部署状态

- 网页：打开仓库 → **Actions** 标签页，看最新一次运行是否绿色
- 命令行（已装 gh CLI）：

```powershell
D:\Code\.tools\gh\bin\gh.exe run list -R Azainl/Azainl.github.io
D:\Code\.tools\gh\bin\gh.exe run watch -R Azainl/Azainl.github.io --exit-status
```

### 7.3 部署配置

自动部署由 `.github/workflows/deploy.yml` 驱动。如果仓库的
**Settings → Pages → Source** 不是 "GitHub Actions"，改成它即可
（目前已经是，一般不需要动）。

### 7.4 回滚

假如某次推送把线上弄坏了：

```powershell
git revert HEAD --no-edit   # 撤销最近一次提交
git push                    # 重新部署回上一个版本
```

### 7.5 自定义域名

1. 在域名服务商把 `blog.yourdomain.com` 的 CNAME 记录指向 `azainl.github.io`
2. 仓库 **Settings → Pages → Custom domain** 填入域名
3. 在 `src/consts.ts` 把 `url` 和 `astro.config.mjs` 的 `site` 改成新域名，
   重新推送

---

## 8. 日常维护清单

- **写文章**：见第 3 节，改完记得推送
- **改信息**：见第 4 节
- **换主题**：见第 5 节
- **看访问情况**：目前站点没有加统计脚本（隐私优先）。以后想加，
  可在 `BaseLayout.astro` 的 `<head>` 里插入统计代码
- **备份**：本地目录 + GitHub 仓库就是双备份，`dist/` 可以随时重建，不需要备份
- **文章数多了之后**：首页会自动按年份分组，无需改动

---

## 9. 发布前的质量检查（可选）

项目里带了两个验证脚本，需要先启动本地预览服务器：

```powershell
npm.cmd run preview
```

然后另开一个终端：

```powershell
node .\scripts\check.mjs        # 自动检查布局、字体、主题、链接、RSS
node .\scripts\screenshot.mjs   # 生成各页面截图到 .shots/
```

检查项包括：页面有无横向溢出、字体是否加载、深浅主题是否生效、
导航是否换行、全站链接是否可达、RSS 是否正常。发布前跑一遍更稳妥。

---

## 10. 常见问题（FAQ）

### npm 提示"禁止运行脚本" / 找不到 npm 命令

本机 PowerShell 执行策略限制，改用 `npm.cmd`：

```powershell
npm.cmd install
npm.cmd run dev
```

### git 提示"无法识别" / 不是内部或外部命令

Git 已安装到 `D:\Code\.tools\git` 并加入用户 PATH。**新开的终端窗口**即可直接
使用 `git`；如果当前窗口还报错，关掉重开一个终端。验证方法：

```powershell
git --version
```

推送时不需要再输入密码，凭据由已授权的 gh CLI 自动提供。

### 推送时报 502 / 无法访问 GitHub

本机 GitHub 流量会被本地加速工具（steamcommunity_302 等）接管，它的上游
故障时会返回 502。两个解决办法：

```powershell
# 方法一：临时走 Clash 代理推送（推荐，无需动任何工具）
git -c http.proxy=http://127.0.0.1:7897 push

# 方法二：重启 steamcommunity_302 后恢复正常，再直接 git push
```

如果换了代理端口，把命令里的 `7897` 换成实际端口即可。

### 端口被占用，dev 服务器起不来

```powershell
npm.cmd run dev -- --port 3000
```

### 构建报错（比如日期格式）

frontmatter 校验失败。看报错提示，检查 `date` 是否为 `YYYY-MM-DD`、
必填字段是否齐全。改完重新构建即可。

### git 提示 CRLF / LF 警告

Windows 行尾转换的提示，**无害**，不影响任何功能。

### 推送被拒绝：缺少 workflow 权限

首次推送 `.github/workflows/` 目录时需要 workflow 权限。已授权过，
如果以后换新电脑，用 gh CLI 登录后执行：

```powershell
D:\Code\.tools\gh\bin\gh.exe auth refresh -h github.com -s workflow
```

### 改了代码但线上没变化

1. 确认执行了 `git push` 且 Actions 运行是绿色（见 7.2）
2. 浏览器强制刷新（Ctrl+F5），GitHub Pages 有 CDN 缓存
3. 检查是否把 `draft` 写成了 `true`

### 怎么停掉本地预览服务器

在任务管理器里结束对应的 `node` 进程，或关掉运行它的终端窗口。

### 想迁移到 Vercel / Netlify

项目已内置 `vercel.json` 和 `netlify.toml`，把仓库导入对应平台即可，
构建命令 `npm run build`，发布目录 `dist`。

---

## 11. 技术栈速览

| 部分 | 技术 |
| --- | --- |
| 框架 | Astro 5（静态站点） |
| 内容 | Markdown + Content Collections（Zod 校验） |
| 样式 | 手写 CSS 设计令牌（无框架依赖） |
| 字体 | Geist、Source Serif 4（自托管可变字体） |
| 图标 | Phosphor Icons |
| 代码高亮 | Shiki |
| 部署 | GitHub Actions → GitHub Pages |
| SEO | 自动生成 RSS、sitemap、OG 标签 |

---

> 文档维护：修改站点结构后，记得同步更新本文件。
